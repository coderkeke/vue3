# Project Rules

- Vue Template 属性顺序符合 vue/attributes-order 规范 (DEFINITION -> LIST -> CONDITIONALS -> RENDER -> GLOBAL -> UNIQUE -> SLOT -> TWO_WAY -> DIRECTIVES -> ATTRS -> EVENTS -> CONTENT)
- Vue Template 属性名必须使用 kebab-case (vue/attribute-hyphenation)
- 样式使用 less 编写
- 禁止使用 any 类型
- 代码在关键位置要有中文注释
